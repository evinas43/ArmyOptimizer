<Application x:Class="ArmyOptimizer.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ArmyOptimizer"
             xmlns:views="clr-namespace:ArmyOptimizer.Views"
             xmlns:vm="clr-namespace:ArmyOptimizer.ViewModels"
             xmlns:utils="clr-namespace:ArmyOptimizer.Utilities"
             StartupUri="MainWindow.xaml">
    
    <Application.Resources>

        <DataTemplate DataType="{x:Type vm:LoginVM}">
            <views:LoginView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:RegisterVM}">
            <views:RegisterView/>
        </DataTemplate>
        
        <DataTemplate DataType="{x:Type vm:HomeVM}">
            <views:HomeView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:ArmyDetailVM}">
            <views:ArmyDetailView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:OptimizeArmyVM}">
            <views:OptimizeArmyView/>
        </DataTemplate>
        
        <DataTemplate DataType="{x:Type vm:SaveArmyVM}">
            <views:SaveArmyView/>
        </DataTemplate>
        
        
        
        <utils:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        



        <Style x:Key="SummaryCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#111C2D"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="20"/>
        </Style>

        <Style x:Key="SectionCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#111C2D"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Padding" Value="25"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>

        <Style x:Key="ArmyProgressBarStyle" TargetType="ProgressBar">

            <Setter Property="Foreground" Value="#F59E0B"/>
            <Setter Property="Background" Value="#1A2433"/>
            <Setter Property="Height" Value="12"/>

            <Setter Property="Template">
                <Setter.Value>

                    <ControlTemplate TargetType="ProgressBar">

                        <Border Background="{TemplateBinding Background}"
                        CornerRadius="10">

                            <Grid>

                                <Border x:Name="PART_Track"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"/>

                                <Border x:Name="PART_Indicator"
                                Background="{TemplateBinding Foreground}"
                                CornerRadius="10"
                                HorizontalAlignment="Left"/>

                            </Grid>

                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter TargetName="PART_Indicator"
                Property="Width"
                Value="{Binding RelativeSource={RelativeSource TemplatedParent},
                Path=ActualWidth}"/>
                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>

                </Setter.Value>
            </Setter>

            <Style.Triggers>

                <!-- Army -->
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="Army"/>
                        <Condition Binding="{Binding IsArmyFull}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Foreground" Value="Green"/>
                </MultiDataTrigger>

                <!-- Spell -->
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="Spells"/>
                        <Condition Binding="{Binding IsSpellFull}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Foreground" Value="Green"/>
                </MultiDataTrigger>

                <!-- Siege -->
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="SiegeMachines"/>
                        <Condition Binding="{Binding IsSiegeFull}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Foreground" Value="Green"/>
                </MultiDataTrigger>

            </Style.Triggers>

        </Style>

    </Application.Resources>
   
</Application>
